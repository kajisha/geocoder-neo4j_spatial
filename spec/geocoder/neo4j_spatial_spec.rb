require 'spec_helper'

describe Geocoder::Neo4jSpatial do
  it 'has a version number' do
    expect(Geocoder::Neo4jSpatial::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
